# idea-launch

Investor-Ready Dashboard & Analytics

## Usage

1. Run `python -m pytest` to test the implementation.
2. Use the `DemoButton` class to create a live demo button.
3. Style the button using the `get_styled_button` function.
4. Refresh the demo URL using the `refresh_demo_url` function.
